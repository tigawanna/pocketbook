interface pageProps {}

export default async function PostPage({}: pageProps) {
  return (
    <main className="w-full h-full min-h-screen flex flex-col items-center">
      create post
    </main>
  );
}
